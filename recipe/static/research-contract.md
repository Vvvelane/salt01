# Recipe research contract

Research Audit accepts only a `recipe-research-v1` manifest from a configured
read-only source. Missing research data is a valid `no_data` state. Recipe
does not generate factors, signals, trades, shortlist rows, or PnL.

